# 004. GDR 運用の課題と発展性

## 目次

**GDR 一覧:**

| GDR ID | 決定の要約 | status |
|---|---|---|
| GDR-META-009 | GDR にライフサイクルステータスを導入する | Implemented |
| GDR-META-010 | 大規模プロジェクト向けに検索性を強化する | Proposed |
| ~~GDR-META-011~~ | ~~GDR 化すべき判断の粒度ガイドラインを策定する~~ | 対応不要 |

**セクション:**

1. [GDR（General Decision Record）](#1-gdrgeneral-decision-record)
2. [現状](#2-現状)
3. [改善案](#3-改善案)
4. [詳細セクション](#4-詳細セクション)
5. [検討課題](#5-検討課題)
6. [実行計画](#6-実行計画)
7. [ふりかえり](#7-ふりかえり)

---

## 1. GDR（General Decision Record）

**GDR-META-009: GDR にライフサイクルステータスを導入する**

- **status:** Implemented
- **scope:** meta
- **決定:** GDR レコードに `Proposed`（提案中）/ `Approved`（承認済み）/ `Implemented`（実装済み）/ `Superseded`（代替済み）のステータスを導入する。ある GDR が別の GDR を上書き（Supersede）した場合、相互にリンクを貼る
- **理由:** プロジェクトが長期化すると古い GDR と新しい GDR が矛盾する可能性がある。ADR に倣いステータスを明示することで、どの判断が現在有効かを追跡できる
- **影響:** GDR 書式にステータスフィールドを追加する。01_GENERAL_DECISION_RECORD.md の書式定義を更新する
- **再検討条件:** ステータス管理のオーバーヘッドが判断の記録速度を阻害する場合

**GDR-META-010: 大規模プロジェクト向けに検索性を強化する**

- **status:** Proposed
- **scope:** meta
- **決定:** `scope` / `PREFIX` に加えて自由タグによる分類を可能にする。AI が `GDR_INDEX` を自動更新し、要約と関連リンクを生成するプロンプトを整備する
- **理由:** レコードが数百件になった際、既存の `scope` / `PREFIX` だけでは過去の判断を迅速に参照できなくなる
- **影響:** GDR 書式にタグフィールドを追加する。GDR_INDEX 自動生成のプロンプトを 03_PROMPT_GUIDE.md に追加する
- **再検討条件:** 実際にレコード数が 100 件を超え検索性の問題が顕在化した場合

~~**GDR-META-011: GDR 化すべき判断の粒度ガイドラインを策定する**~~ — 対応不要

[↑ 目次へ](#目次)

---

## 2. 現状

1. GDR レコードにステータスがなく、古い判断と新しい判断の有効性が区別できない
2. レコード数が増加した場合、`scope` / `PREFIX` だけでは過去の判断を迅速に検索できない

[↑ 目次へ](#目次)

---

## 3. 改善案

GDR にライフサイクルステータス（Proposed / Approved / Implemented / Superseded）を導入し、判断の有効性を追跡可能にする。将来的にはタグベースの検索性強化も行う。

[↑ 目次へ](#目次)

---

## 4. 詳細セクション

### 4.1. ライフサイクルステータス

GDR レコードに以下のステータスフィールドを追加する:

- `Proposed` — 提案中。まだ合意されていない
- `Approved` — 承認済み。実装待ち
- `Implemented` — 実装済み。現在有効
- `Superseded` — 代替済み。代替先 GDR ID を記載し相互リンクする

### 4.2. 検索性強化（保留）

`scope` / `PREFIX` に加えて自由タグを導入し、AI による `GDR_INDEX` 自動更新を整備する。レコード数が 100 件を超えた時点で着手する。

[↑ 目次へ](#目次)

---

## 5. 検討課題

1. **ステータス管理のオーバーヘッド**
   - 解決策: ステータスは GDR 作成時に `Proposed` をデフォルト設定し、実装完了時に `Implemented` に変更するだけの最小運用とする
   - → GDR-META-009 の再検討条件として記録済み
2. **検索性強化の着手タイミング**
   - 解決策: レコード数 100 件を閾値とし、それまでは現行の `scope` / `PREFIX` で運用する
   - → GDR-META-010 の再検討条件として記録済み

[↑ 目次へ](#目次)

---

## 6. 実行計画

### 6.1. タスク一覧

| # | タスク | 根拠 GDR | 依存 | ステータス |
|---|---|---|---|---|
| 1.1 | GDR 書式にステータスフィールドを追加 | GDR-META-009 | — | 完了 |
| 1.2 | Supersede 時の相互リンクルールを明文化 | GDR-META-009 | — | 完了 |
| 2.1 | タグフィールドの書式を設計 | GDR-META-010 | — | 保留 |
| 2.2 | GDR_INDEX 自動生成プロンプトを整備 | GDR-META-010 | 2.1 | 保留 |

### 6.2. フェーズ詳細

#### フェーズ 1: ライフサイクルステータスの導入 ✅

**目的:** GDR レコードの有効性を追跡可能にする

- [x] 1.1 01_GENERAL_DECISION_RECORD.md の書式にステータスフィールドを追加
- [x] 1.2 Supersede 時の相互リンクルールを 01_GENERAL_DECISION_RECORD.md の書式説明に明文化

---

#### フェーズ 2: 検索性の強化（保留）

> レコード数が 100 件を超え検索性の問題が顕在化した時点で着手する。

- [ ] 2.1 タグフィールドの書式を設計
- [ ] 2.2 GDR_INDEX 自動生成プロンプトを整備

### 6.3. 進捗サマリー

| フェーズ | タスク数 | 完了 | 残 | コミット |
|---|---|---|---|---|
| 1 | 2 | 2 | 0 | — |
| 2 | 2 | 0 | 2 | — |

[↑ 目次へ](#目次)

---

## 7. ふりかえり

### 7.1. 観察された傾向

1. **フレームワークの自己進化** — GDR フレームワークの評価プロセスそのものが、フレームワーク自体のメタ的な改善（GDR-META シリーズ）に直結している
2. **AI-Driven の優位性** — 人間が見落としがちな将来の管理コスト（ライフサイクルや検索性）について、AI が構造的に指摘できている

### 7.2. 次回への申し送り

- ステータス管理（GDR-META-009）は文書の整合性維持に直結するため優先度が高い
- 検索性強化（GDR-META-010）はレコード数が 100 件を超えた時点で再検討する
- 判断粒度ガイドライン（GDR-META-011）は対応不要と判断

[↑ 目次へ](#目次)
